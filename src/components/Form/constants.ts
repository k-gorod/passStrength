import { IRules } from './types';

export const colors: string[] = [
  'success',
  'danger',
  'warning',
  'success',
];

export const initialRules: IRules = {
  longEnough: null,
  oneUpperCase: null,
  oneLowerCase: null,
  oneNumber: null,
};
